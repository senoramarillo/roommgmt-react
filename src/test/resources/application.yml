spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:h2:file:./db/springdb
    driver-class-name: org.h2.Driver
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: true